{% macro getPreferedUrl(arr) %}
  {# if open access exist print that url otherwise print protected url #}
  {% if count(arr) >= 1 %}
      {% if arr[0] is not empty %}
        {{arr[0].field_url.url.value}}
      {% endif %}
  {% endif %}
{% endmacro %}



