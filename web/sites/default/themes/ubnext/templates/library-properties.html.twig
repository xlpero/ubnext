{# slate
  name = Library properties/features
  models[library] = entity:node
#}
{% import "macros/properties.html.twig" as properties %}
{{ properties.properties(t("At !location", {'!location' : library.title_field.value}), library.ubn_property_fields) }}
