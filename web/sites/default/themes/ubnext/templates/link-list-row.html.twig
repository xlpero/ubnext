{# slate
  name = A list of links: links
  models[list] = entity:node
#}

{% import "macros/panel-links.html.twig" as panelLinks %}
{{ panelLinks.get_link_list(list.title_field.value, list.field_shortcut_links) }}
