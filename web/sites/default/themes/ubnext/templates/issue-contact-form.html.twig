{# slate
  name = Page: issue contact form
  models[page] = entity:node
#}

{# import "macros/entityforms.html.twig" as entityforms #}
{% if not page.field_issue_form_instance is empty %}
  {% set instance = page.field_issue_form_instance %}
  {{
    ubn_form_instance_render(instance, 'issue') | raw
  }}
{% endif %}
