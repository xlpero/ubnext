{# slate
  name = Library: Library Map
  models[map] = item
  models[libraries] = item
#}
<div class="library-map">
	<div class="header">
        {% if count(libraries) > 1 %}
            <div class="links">
                <select id="library-links" class="form-control">
                        <option selected>Välj bibliotek:</option>
                    {% for library in libraries %}
                        <option value="{{ library.url }}">{{ library.label }}</option>
                    {% endfor %}
                </select>
            </div>
        {% else %}
            <h3>{{ libraries[0].label }}</h3>
        {% endif %}
    </div>
    {{ map | raw }}
</div>
