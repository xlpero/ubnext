<?php

function ubn_general_install() {
  theme_enable(array('ubnext'));
  theme_disable(array('bartik'));
  $modules = array('dashboard', 'overlay', 'shortcut', 'toolbar');
  module_disable($modules);
  drupal_uninstall_modules($modules);
}
